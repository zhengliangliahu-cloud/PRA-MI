trainer:
  engine: lightning
  accelerator: cpu
  devices: 1
  max_epochs: 3
  batch_size: 32
  num_workers: 0
  val_subjects: 1
  pin_memory: false
  precision: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  deterministic: true
  log_every_n_steps: 5
