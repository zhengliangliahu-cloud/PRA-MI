trainer:
  engine: numpy
  accelerator: cpu
  devices: 1
  max_epochs: 1
  batch_size: 32
  num_workers: 0
  val_subjects: 1
  learning_rate: 0.001
  weight_decay: 0.0
  deterministic: true
  log_every_n_steps: 1
